# Ноутбук с деревьями я сюда не закидывал, потому что там нечего делать.

## Перед запуском ноутбуков
```bash
git clone https://github.com/Xivalor/ml-course-homeworks.git
cd ml-course-homeworks
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
